{
    "services": {
        "frontend": {
            "root": "frontend",
            "framework": "nextjs",
            "runtime": "container",
            "buildCommand": "npm install && npm run build"
        },
        "backend": {
            "root": "backend",
            "framework": "flask",
            "runtime": "container",
            "buildCommand": "pip install -r requirements.txt && python app.py",
            "entrypoint": "app.py"
        }
    },
    "rewrites": [
        {
            "source": "/api/backend(/.*)?",
            "destination": {
                "type": "service",
                "service": "backend"
            }
        },
        {
            "source": "/(.*)",
            "destination": {
                "type": "service",
                "service": "frontend"
            }
        }
    ]
}